const path = require('path');

module.exports = {
  'config': path.resolve('./src/config', 'config.json'),  // Ruta del archivo config.json
  'migrations-path': path.resolve('./src/migrations'),  // Ruta personalizada para migraciones
  'models-path': path.resolve('./src/models')  // Ruta personalizada para modelos, si lo necesitas
};
